/**
 * Demo: package.json scripts — the single entry point for every project task.
 * Run: node module-01-modern-javascript/demo/03-package-scripts
 *
 * Walk-through:
 *   1. `npm init -y` (or `pnpm init`) to scaffold a package.json
 *   2. Add a "start" script that runs this file
 *   3. `pnpm run start` (or just `pnpm start`) to execute it
 *
 * Key points:
 *   - Scripts run with node_modules/.bin on the PATH
 *   - `start` and `test` are special — no `run` needed
 *   - Chain commands with && for multi-step workflows
 */

console.log('DINO_SAFARI_TRACKER_ONLINE');
console.log('All perimeter sensors reporting.');
