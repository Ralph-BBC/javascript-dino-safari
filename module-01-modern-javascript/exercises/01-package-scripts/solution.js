/**
 * Instructor reference — paste into package.json "scripts" (or compare yours).
 */
export const referenceScripts = {
  start: 'node tracker.js',
  lint: 'eslint tracker.js',
  test: 'echo "Run from repo root: npx vitest run module-01-modern-javascript/exercises/01-package-scripts/start.test.js"',
};
